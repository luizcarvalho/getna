class Create<%=name[:class_plural] %>    < ActiveRecord::Migration
  def self.up
  create_table <%=name[:plural]  %>, :force => true do |t|
  <%  attrs.each do |attr| %>
  t.<%= attr[:type] %> :<%= attr[:nome] if attr[:nome]%>, :limit=><%= attr[:limit] if attr[:limit]%> , :null=> <%= attr[:null] if attr[:null]%>, :default=><%= attr[:default] if attr[:default]%>
  <% end %>
    end
  end
  
  def self.down
  drop_table : <%=  name[:plural]%>  
  end
end
