class Create<%=object_name[:class_plural] %> < ActiveRecord::Migration
  def self.up
  create_table :<%=object_name[:plural]  %>, :force => true do |t|
  <% attributes.each do |attr| %>
  t.<%= attr[:type] %> <%= ":#{attr[:name]}" unless attr[:name].nil?%> <%= ", :limit=>#{attr[:limit]}" unless attr[:limit].nil? %><%= ", :null=>#{attr[:null]}" unless attr[:null].nil? %><%= ", :default=>#{attr[:default]}" unless attr[:default].nil? %>
  <% end %>
    end
  end
  
  def self.down
  drop_table :<%=object_name[:plural]%>  
  end
end
